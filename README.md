# 1803APP

## Deployment

This repository deploys from the repo root on Vercel. The root `package.json` proxies build, dev, test, and typecheck commands into `web/`, and the root `vercel.json` publishes `web/dist`.
